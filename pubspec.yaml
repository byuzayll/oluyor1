name: oluyor1
description: "Yeni bir Flutter projesi."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.5.0-323.2.beta <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_localizations:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  generate: true
  assets:
    - assets/kutso-kariyer-banner-1.png
    - assets/kutso.png